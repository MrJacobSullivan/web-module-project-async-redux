import tw, { styled } from 'twin.macro'

const Container = styled.section`
  ${tw``};
`

const Image = styled.img`
  ${tw``};
`

const Title = styled.h2`
  ${tw``};
`

const Date = styled.p`
  ${tw``};
`

const Button = styled.button`
  ${tw``};
`

export { Container, Image, Title, Date, Button }
